name: geomrel
channels: [pytorch, nvidia, conda-forge]
dependencies:
  - python=3.11
  - pip
  - numpy=2.4.2
  - scipy=1.17.1
  - scikit-learn=1.8.0
  - pandas
  - pytorch=2.10.0
  - torchvision
  - pytorch-cuda=12.1
  - statsmodels
  - matplotlib
  - seaborn
  - pyyaml
  - tqdm
  - pip:
      - timm==0.9.16
      - open_clip_torch==2.32.0
      - transformers==5.14.1
      - pingouin
# PINNED per D-044/D-045. timm held at 0.9.16 for all of Paper 5.
# Original scaffold note: pin every one of them before the
# first run that produces a reportable number, and record the resolved versions
# in results/manifest.jsonl per run.
