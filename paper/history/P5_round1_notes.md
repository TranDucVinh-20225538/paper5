# P5 round 1

Source tagged `p5-round1`. Protocol frozen at `a83c74c` (`v0.2-protocol-frozen`).

## What changed since round 0

- Manuscript converted from Markdown to LaTeX. The Markdown draft was **removed**
  rather than kept: two editable copies of one manuscript is the failure mode this
  repository warns about elsewhere, and the `.tex` is now the single source.
- Confirmatory analysis reconstructed from the frozen specification after the
  original run was found to exist on no branch (D-053). It reproduces the reported
  table exactly, so the result is now regenerable from version-controlled inputs
  rather than resting on a single artifact.
- Two figures added, both generated from committed outputs.
- Bibliography scaffolded with stable keys; author, venue and year are TODO.

## Still open

- Citations `paper1`–`paper4` carry placeholder fields.
- Title is a draft.
- `LICENSE` is blocked on the MONET ShareAlike question (D-046), not on indecision.

## Result, unchanged from round 0

Outcome D, heterogeneous. Replication on the original backbone exact
(τ = +0.5576, Δ = 0.0000). Deviations from the preregistration: none.
