# Production pipeline queue — processed top-to-bottom by run_production_queue.sh
#
# Backbone readiness (2026-08-12):
#   monet, biomedclip     READY (preprocessing + SHA256 frozen)
#   openclip, siglip      checkpoint pinned; preprocessing SHA256 pending
#   mocov3                D-042 provenance verify pending (not blocked on download)
#   medsam                design locked (D-025–D-027); config/preprocessing pin pending in repo
#   dinov3                DEFERRED — HF gating pending Meta approval (see configs/dinov3.yaml)
#
# Failures log to results/scheduler/failures.jsonl; queue continues.
#
# Completed (do not rerun):
#   panderm, resnet50, efficientnet_b3
#
# Running outside scheduler:
#   uni (sequential rerun — waiter blocks until GPU free)
#
configs/monet.yaml
configs/biomedclip.yaml
configs/openclip.yaml
configs/siglip.yaml
configs/mocov3.yaml
configs/medsam.yaml
# configs/dinov3.yaml  DEFERRED — uncomment after HF access approved + verify_checkpoint
